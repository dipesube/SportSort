package com.example.sportsort;

import java.util.ArrayList;
import java.util.Arrays;
import com.example.sportsort.*;

public class Test{

		String name = "Paul Blart";
		ArrayList<String> interests = new ArrayList<String>(Arrays.asList("mall cop", "airplane instruction videos from the 80's", "Jihad"));
		ArrayList<Integer> skillLevel = new ArrayList<Integer>(Arrays.asList(200, 5, 21221));
		short endorsement = 1;
		ArrayList<String> currentEvents = new ArrayList<String>(Arrays.asList("conference", "rallying", "buying a noose"));
		ArrayList<String> previousEvents = new ArrayList<String>(Arrays.asList("tied a noose", "had kids", "kids estranged"));
		boolean privacy = false;
		
		//User tester = new User(name,interests,skillLevel,endorsement,currentEvents,previousEvents,privacy);
		
		//FileIOUser.writeNewUser("testText.txt", tester);

}
